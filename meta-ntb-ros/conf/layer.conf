BBPATH =. "${LAYERDIR}:"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "ntb-ros"

BBFILE_PATTERN_ntb-ros := "^${LAYERDIR}/"
BBFILE_PRIORITY_ntb-ros := "5"
